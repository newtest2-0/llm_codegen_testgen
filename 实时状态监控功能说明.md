# 🔄 实时状态监控功能

## 功能概述

实现了完整的实时系统状态监控功能，可以在界面上实时显示API调用延迟、队列状态和系统指标。

## 🎯 监控指标

### 主要指标
- **API延迟**：实时显示API调用的响应时间（毫秒）
- **队列大小**：当前正在处理的请求数量
- **总请求数**：系统启动以来的总请求数
- **平均响应时间**：最近50次请求的平均处理时间
- **可用提供者**：当前可用的AI模型提供者数量
- **系统状态**：在线/离线状态及提供者信息

### 颜色编码
- **绿色**：性能良好（延迟 < 100ms，队列 = 0）
- **黄色**：性能一般（延迟 100-500ms，队列 1-2）
- **红色**：性能较差（延迟 > 500ms，队列 > 2）

## 🔧 技术实现

### 后端实现

#### 1. 系统指标跟踪 (`backend/api/routes/system.py`)
```python
class SystemMetrics:
    - active_requests: 当前活跃请求数
    - total_requests: 总请求数
    - response_times: 响应时间历史
    - average_response_time: 平均响应时间
```

#### 2. API端点
- `GET /api/v1/system/metrics` - 获取实时系统指标
- `GET /api/v1/system/ping` - 简单ping测试延迟

#### 3. 请求跟踪
在代码生成API中集成了请求开始和结束的跟踪，自动记录每次请求的处理时间。

### 前端实现

#### 1. 实时监控 (`web/static/js/main.js`)
```javascript
// 每3秒自动更新一次系统指标
setInterval(updateSystemMetrics, 3000)

// 智能页面可见性控制
document.addEventListener('visibilitychange', ...)
```

#### 2. UI更新
- 动态更新延迟、队列、请求数等指标
- 根据性能水平自动调整颜色显示
- 实时显示系统在线状态和可用提供者

#### 3. 性能优化
- 页面隐藏时自动停止监控以节省资源
- 页面重新可见时自动恢复监控
- 页面卸载时清理定时器

## 📊 界面显示

### 系统状态卡片
```
┌─────────────────────────┐
│ 🖥️ 系统状态             │
├─────────────────────────┤
│ ● 服务在线              │
│ 模型: openai, claude... │
│ 8 个提供者可用          │
├─────────────────────────┤
│  延迟     │    队列     │
│  45 ms    │     2       │
├─────────────────────────┤
│ 总请求    │  平均响应   │
│   156     │   67 ms     │
└─────────────────────────┘
```

## 🚀 使用方法

### 自动监控
- 页面加载后自动开始实时监控
- 无需手动操作，后台自动更新

### 手动控制
- 可通过JavaScript控制监控的开始和停止
- 支持自定义监控间隔时间

### 测试功能
- 提供了独立的测试页面 `test_realtime_status.html`
- 可以手动测试各项监控功能

## 🔍 监控数据流

```
1. 前端发起请求 → /api/v1/system/metrics
2. 后端记录请求开始时间
3. 收集系统指标数据
4. 计算延迟和队列状态
5. 返回JSON格式的指标数据
6. 前端更新UI显示
7. 每3秒重复执行
```

## 📈 性能特点

- **低延迟**：监控请求本身延迟极低（通常 < 10ms）
- **高效率**：页面隐藏时自动停止监控
- **准确性**：结合客户端和服务端延迟测量
- **实时性**：3秒更新间隔，平衡实时性和性能
- **智能化**：根据性能自动调整显示颜色

## 🎯 应用场景

1. **开发调试**：实时监控API性能，快速发现问题
2. **系统运维**：监控服务器负载和响应时间
3. **用户体验**：让用户了解系统当前状态
4. **性能优化**：通过历史数据分析性能趋势

这个实时监控系统为您的代码生成平台提供了完整的性能可视化，让系统状态一目了然！
